// .sequelizerc

const path = require("path");

module.exports = {
  config: path.resolve("src", "sequelize-config.json"),
  "models-path": path.resolve("src", "models"),
  "migrations-path": path.resolve("src", "migrations"),
};
